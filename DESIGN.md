# Shadow Mitts - 설계서 (로컬 Claude Code용)

웹캠으로 사용자의 자세(pose)를 인식해서, 날아오는 미트를 실제 펀치로 치는 복싱 연습 웹 게임.
키보드/마우스 조작 없음. 영상은 브라우저 안에서만 처리하고 서버로 보내지 않는다.

## 0. Claude Code에 처음 붙여넣을 프롬프트

```
이 DESIGN.md를 읽고 프로젝트를 만들어줘. 규칙:
1) DESIGN.md의 "확인됨 / 미확인" 표를 지켜라. 미확인 항목은 추측으로 코딩하지 말고,
   설치한 패키지의 타입 정의(.d.ts)나 실제 실행 결과로 먼저 확인해라.
2) 마일스톤 M0부터 순서대로 진행하고, 각 마일스톤이 끝나면 내가 웹캠으로 직접 테스트할 수 있게
   실행 방법과 "무엇을 확인해야 하는지"를 알려줘. 내 확인 없이 다음 마일스톤으로 넘어가지 마라.
3) 펀치 인식 임계값은 전부 placeholder다. 코드에 상수로 모으고 화면에서 슬라이더로 조정 가능하게 해라.
4) 인식 로직은 카메라 없이도 테스트할 수 있어야 한다(녹화 JSON 재생 + 합성 데이터 단위 테스트).
```

## 1. 콘셉트와 차별점 (첫 버전 범위)

이미 비슷한 앱이 많으므로 "뻔한 것"은 뒤로 미루고 아래에 집중한다.

1. **실제 복싱 번호 콤보**: 1=리드 잽, 2=리어 크로스, 3=리드 훅, 4=리어 훅, 5=리드 어퍼컷, 6=리어 어퍼컷.
   "1-2-3" 같은 콤보를 출제한다. 오소독스/사우스포 설정을 지원한다.
2. **가드 채점**: 치지 않는 손이 턱 근처에 있는지 판정한다. 가드를 내리면 보너스를 못 받는다.
3. **약점 적응형 출제**: 펀치 종류별 정확도를 기록해서 약한 펀치를 더 자주 낸다. 결과 화면에 약점 리포트.
4. **타격감**: 히트 이펙트, 화면 흔들림, WebAudio로 합성한 타격음(외부 오디오 에셋 없음).
5. **기록/공유**: 점수, 콤보, 정확도, 펀치별 통계, 결과 카드 PNG 저장.

첫 버전에서 **제외**: BPM 리듬 싱크, 보스전/스토리, 대전 모드(슬립/더킹 판정 필요), 서버/계정.

## 2. 기술 스택

- 순수 웹(정적 호스팅). 빌드 도구는 선택 사항(Vite 등). 프레임워크 없이 Canvas 2D로 충분하다.
- 포즈 인식: MediaPipe Pose Landmarker (`@mediapipe/tasks-vision`), 브라우저에서 실행.
  YOLO-pose는 웹에서 무겁고 손목/팔꿈치 정확도 면에서 이 용도에 이점이 없다는 것이 내 판단(측정한 것은 아님).
- 카메라는 `getUserMedia`. HTTPS 또는 localhost에서만 동작한다(브라우저 정책).
- 배포: GitHub Pages 같은 정적 호스팅이면 충분.

## 3. 확인됨 / 미확인

2026-09-23에 공식 문서 페이지를 읽고 확인한 내용이다. 문서를 요약 도구로 읽은 것이므로
코딩 전에 설치된 패키지의 타입 정의로 한 번 더 대조할 것.

| 항목 | 상태 | 내용 |
|---|---|---|
| CDN 로드 | 문서 확인 | `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision/vision_bundle.mjs` |
| WASM 경로 | 문서 확인 | `FilesetResolver.forVisionTasks("https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/wasm")` |
| 모델 URL (lite) | 문서 확인 | `https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/latest/pose_landmarker_lite.task` |
| 모델 URL (full/heavy) | 문서 확인 | 위 URL에서 `lite`를 `full` 또는 `heavy`로 바꾼 형태 |
| createFromOptions 옵션 | 문서 확인 | `baseOptions.modelAssetPath`, `runningMode`(IMAGE/VIDEO), `numPoses`, `minPoseDetectionConfidence`, `minPosePresenceConfidence`, `minTrackingConfidence`, `outputSegmentationMasks` |
| 결과 필드 | 문서 확인 | `landmarks`(정규화 좌표), `worldLandmarks`(미터 단위 3D) |
| 랜드마크 번호 | 문서 확인 | 0 코, 9/10 입(좌/우), 11/12 어깨(좌/우), 13/14 팔꿈치, 15/16 손목, 23/24 엉덩이 (좌/우 순) |
| `delegate: "GPU"` 옵션 | **미확인** | 해당 문서 페이지에는 없었다. 타입 정의에서 확인하고, GPU 실패 시 CPU 폴백을 넣을 것 |
| `detectForVideo` 시그니처 | **미확인** | 문서 예시는 `detectForVideo(video)`였으나 실제로는 타임스탬프(ms) 인자가 필요할 가능성이 높다. 타입 정의로 확인 |
| 월드 좌표의 원점/축 방향 | **미확인** | 문서에 명시 없음. 디버그 화면에 x/y/z를 찍어 직접 움직여 보며 확인할 것(앞으로 뻗을 때 z의 부호 등) |
| 좌/우 라벨과 거울 표시 | **미확인** | 인덱스 15는 "사람의 왼쪽 손목". 화면을 거울로 뒤집어 그릴 때 좌우가 뒤바뀌지 않게 주의하고 실제로 검증 |
| 프레임레이트/지연 | **미확인** | 기기마다 다르다. FPS와 카메라~화면 지연을 화면에 표시하고 측정할 것 |

## 4. 펀치 인식 설계 (가장 어려운 부분)

**현실적인 한계**: 웹캠 1대로는 앞뒤 방향 깊이가 약하다. 직선 펀치(잽/크로스)는 팔이 카메라 쪽으로
뻗어서 2D 화면상 변화가 작고, 훅/어퍼컷 구분도 오인식이 많을 수 있다. 처음부터 완벽한 분류를 목표로 하지 말고,
"펀치를 쳤는가 + 어느 손인가"를 먼저 안정화한 뒤 종류 구분을 점차 정교하게 한다.

**입력 정규화**
- 손목 좌표를 같은 쪽 어깨 기준 상대 좌표로 바꾸고, 어깨 너비로 나눠 카메라 거리에 무관하게 만든다.
- 시작 시 캘리브레이션(가드 자세 3초 + 각 팔 최대로 뻗기)으로 사용자별 팔 길이/기준 자세를 저장한다.
- 프레임 노이즈는 One Euro 필터나 EMA로 완화한다.

**검출 (팔마다 독립)**
- 손목 속도가 높은 임계값을 넘고 몸에서 멀어지는 방향이면 펀치 시작.
- 속도가 낮은 임계값 아래로 떨어지면 종료(히스테리시스). 종료 직후 짧은 쿨다운으로 중복 카운트 방지.
- 펀치 이벤트는 `{ 타임스탬프, 손, 종류, 신뢰도, 특징값 }`으로 내보낸다.

**분류 (초기 규칙, 전부 튜닝 대상)**
- 직선: 팔꿈치 각도가 크게 펴지고(예: 150도 이상, placeholder) 변위가 전방/깊이 성분 위주.
- 훅: 수평(x) 변위가 지배적이고 팔꿈치가 접힌 채(예: 70~120도, placeholder).
- 어퍼컷: 수직(y, 위쪽) 변위가 지배적이고 팔꿈치가 접힌 채.
- 번호 매핑: 리드/리어 손 x 직선/훅/어퍼 조합 -> 1~6.

**가드 판정**
- 치지 않는 손목이 턱/입(랜드마크 9, 10) 높이 근처이고 얼굴에서 너무 멀지 않으면 가드 OK. 임계값은 튜닝.

**품질 관리**
- 랜드마크 visibility가 낮으면 그 프레임의 해당 팔은 무시한다.
- 상체(양 어깨\~손목)가 화면에 다 들어와야 한다. 시작 화면에서 "1.5\~2m 뒤로, 상체가 보이게" 안내하고 실시간 체크.
- 조명이 어둡거나 배경과 옷이 비슷하면 인식이 나빠진다는 안내를 넣는다.

## 5. 게임 설계 (미트 타이밍 모드)

- 판정은 **이벤트 기반**: 미트가 도착 시각 t_hit에 닿는 순간 가까운 시각에 올바른 손/종류의 펀치 이벤트가 있었는지로 판정한다.
  (손 위치가 미트 좌표에 닿았는지로 판정하는 방식은 웹캠 깊이 한계 때문에 부정확하므로 쓰지 않는다.)
- 판정 등급: Perfect / Good / Miss. 창 크기는 placeholder(예: Perfect ±80ms, Good ±160ms)이고 난이도별로 조절.
- **지연 보정**: 카메라+추론+렌더 지연 때문에 판정이 밀린다. 설정에 "지연 오프셋(ms)" 슬라이더와 자동 보정 화면을 넣는다.
- 종류가 틀려도 손만 맞으면 부분 점수를 주는 "쉬움" 옵션을 두어, 분류 정확도가 낮은 초기에도 게임이 성립하게 한다.
- 콤보 배율, 가드 유지 보너스, 라운드 구성(예: 3라운드), 난이도(속도/창 크기/콤보 길이).
- 적응형 출제: 펀치 종류별 최근 정확도의 이동평균을 두고, 낮은 종류의 출제 가중치를 높인다.
- 미트 시각화: 화면 앞쪽으로 커지며 다가오는 표적, 번호와 손 색상 표시, 도착 직전 링 수축.
- 이펙트/사운드: 히트 스파크, 화면 흔들림(Perfect 시 강하게), WebAudio 오실레이터/노이즈로 타격음 합성.
- 종료 화면: 점수, 최대 콤보, 정확도, 펀치별 정확도(약점 표시), 칼로리 같은 추정치는 넣지 않거나
  "대략적 추정"이라고 명시(근거 없는 수치를 사실처럼 보여주지 않는다). 결과 카드를 canvas로 그려 PNG 저장.

## 6. 마일스톤 (각 단계 끝에 사용자가 웹캠으로 직접 확인)

- **M0 뼈대와 관측 도구**: 카메라 + 포즈 오버레이 + FPS/지연 표시 + 디버그 패널(손목 속도, 팔꿈치 각도, dx/dy/dz 실시간 표시)
  + 랜드마크 **녹화/재생**(JSON 저장 후 카메라 없이 재생). 월드 좌표 축 방향과 좌/우 라벨을 여기서 확정한다.
- **M1 펀치 검출**: 손/타이밍 검출부터. 녹화 데이터와 합성 데이터 단위 테스트. 종류 분류는 그다음.
- **M2 게임 루프**: 미트 출제, 판정, 점수, 콤보, 가드 보너스, 지연 보정.
- **M3 타격감**: 이펙트, 화면 흔들림, 합성 사운드.
- **M4 성장 요소**: 적응형 출제, 약점 리포트, 결과 카드 PNG, 로컬 기록(localStorage는 실패해도 앱이 동작하게 try/catch).
- **M5 마감**: 온보딩(거리/조명 안내), 접근성(소리 끄기, 화면 흔들림 끄기), 배포.

## 7. 테스트 방침

- 순수 로직(검출, 분류, 판정, 출제)은 DOM/카메라와 분리해서 Node로 단위 테스트한다.
- 웹캠이 필요한 부분(실제 인식 정확도, 지연, 체감)은 자동화할 수 없다. 사용자가 직접 확인하고 임계값을 조정한다.
- "인식이 잘 된다"는 결론은 녹화 데이터나 사용자 테스트 결과가 있을 때만 내린다.

## 8. 알려진 위험

- 훅/어퍼컷 오인식률이 높을 수 있다(웹캠 1대의 한계). 쉬운 판정 옵션과 인식 테스트 모드로 대응.
- 저사양 기기에서 프레임이 낮으면 빠른 펀치를 놓친다. lite 모델 기본, 해상도 낮춤 옵션 제공.
- 좁은 공간, 어두운 조명, 카메라 각도(정면이 아닐 때)에서 성능이 떨어진다.
- 격한 동작이라 주변 안전 안내(주변 공간 확보, 준비운동)를 시작 화면에 넣는다.
